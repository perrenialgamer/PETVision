# Example environment variables
# Copy to `.env` and edit values for local development if needed.

# Backend
BACKEND_PORT=4000
NODE_ENV=development

# ML service (docker-compose service name: ml_service)
ML_API_URL=http://ml_service:8000
ML_SERVICE_PORT=8000

# Frontend
FRONTEND_PORT=5173

# Data / uploads
UPLOAD_DIR=uploads
